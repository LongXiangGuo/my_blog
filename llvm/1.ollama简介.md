# Ollama 完整操作文档（基础介绍 + 命令大全 + Open WebUI可视化管理）
## 一、Ollama 基础介绍
### 1. 什么是 Ollama
Ollama 是开源本地大模型运行工具，**一键管理、加载、运行各类GGUF量化大模型**，自动适配Windows/macOS/Linux显卡（NVIDIA/AMD/Apple Silicon），内置API服务，可搭配Open WebUI实现可视化聊天与模型管理。
核心优势：
- 无需复杂环境配置，单条命令下载运行模型
- 自动显存/内存调度，支持4bit/8bit量化模型，低配电脑可跑
- 内置11434标准API，对接WebUI、Python、第三方工具
- 支持自定义模型、离线导入GGUF模型、模型复制导出

### 2. 核心概念
1. **模型命名规范**：`模型名:量化规格`
    示例：`qwen2.5:7b-q4_K_M`
    - `7b`：70亿参数；数字越大推理越强、占用显存越高
    - `q4_K_M`：4bit中等量化，平衡速度与效果（最通用）
    - `latest`：默认最优量化版本，简写如`ollama pull qwen2.5`
2. **服务端口**：Ollama 默认后台服务端口 `11434`，WebUI通过该端口通信
3. **模型存储路径**
    - Windows：`C:\Users\用户名\.ollama\models`
    - macOS/Linux：`~/.ollama/models`
4. **配套UI：Open WebUI**
    Ollama 官方推荐可视化面板，替代命令行操作，支持：模型下载/删除、多会话聊天、知识库RAG、语音对话、多用户管理、模型参数可视化调节。

### 3. 硬件选型参考（本地运行）
| 模型规格     | 最低显存 | 推荐机型                   |
| ------------ | -------- | -------------------------- |
| 0.5B~3B      | 2G       | 轻薄本、无核显电脑         |
| 7B/8B q4_K_M | 4~6G     | 主流游戏本（RTX3060/4060） |
| 14B q4_K_M   | 8~12G    | RTX4070Ti/3090             |
| 32B q4_K_M   | 16G+     | RTX4090/多卡               |

## 二、Ollama 安装教程
### 1. Windows 安装
方式1（PowerShell一键脚本）
```powershell
irm https://ollama.com/install.ps1 | iex
```
方式2：官网下载exe安装包双击安装，安装后右下角托盘自动启动Ollama后台服务

### 2. macOS / Linux 一键安装
```bash
curl -fsSL https://ollama.com/install.sh | sh
```

### 3. 验证安装成功
打开终端执行：
```bash
ollama --version
```
输出版本号即安装完成。

## 三、Ollama 完整常用命令（分场景）
### （一）模型生命周期管理（最常用）
| 命令                        | 功能                                   | 实操示例                       |
| --------------------------- | -------------------------------------- | ------------------------------ |
| `ollama list` / `ollama ls` | 查看本地全部已下载模型                 | `ollama list`                  |
| `ollama pull 模型名`        | 下载/更新模型（断点续传）              | `ollama pull qwen2.5:7b`       |
| `ollama run 模型名`         | 运行模型，进入终端对话；无模型自动pull | `ollama run llama3.2:8b`       |
| `ollama ps`                 | 查看当前正在加载运行的模型             | `ollama ps`                    |
| `ollama rm 模型名`          | 删除本地模型释放磁盘                   | `ollama rm deepseek-r1:7b`     |
| `ollama cp 源模型 新名称`   | 复制模型（用于自定义微调）             | `ollama cp qwen2.5:7b my-qwen` |
| `ollama show 模型名`        | 查看模型参数、系统提示词、模板         | `ollama show qwen2.5:7b`       |

#### 终端对话操作（run模式内）
- 直接输入文字提问
- `/bye` / `Ctrl+D`：退出对话
- `/set temperature 0.3`：临时调节创意度（0严谨，1脑洞大）
- `/clear`：清空当前对话上下文

### （二）自定义离线GGUF模型（Modelfile）
1. 在GGUF文件同目录新建无后缀文件 `Modelfile`
```
FROM ./qwen2.5-7b-q4_K_M.gguf
PARAMETER temperature 0.7
TEMPLATE "{{ if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}{{ if .Prompt }}<|im_start|>user
{{ .Prompt }}<|im_end|>
{{ end }}<|im_start|>assistant
"
```
2. 导入模型到Ollama库
```bash
ollama create my-qwen7b -f Modelfile
```
3. 运行自定义模型
```bash
ollama run my-qwen7b
```

### （三）模型导入导出（离线迁移）
```bash
# 导出模型为压缩包
ollama export qwen2.5:7b qwen7b.tar
# 离线机器导入
ollama import qwen7b.tar
```

### （四）服务与系统命令
```bash
# 手动启动Ollama后台服务（默认开机自启）
ollama serve

# 查看系统资源占用
ollama system

# 查看模型依赖文件
ollama deps qwen2.5:7b

# 查看全部命令帮助
ollama help
```

### （五）API 调用（程序对接）
Ollama 本地API地址：`http://127.0.0.1:11434`
简单curl测试：
```bash
curl http://localhost:11434/api/generate -d '{
  "model": "qwen2.5:7b",
  "prompt": "写一段Python快速排序代码"
}'
```

## 四、Open WebUI 可视化部署与模型管理（解决你之前拉取ghcr镜像失败问题）
### 方案1：Docker阿里云镜像部署（推荐，规避ghcr.io访问失败）
一键复制运行，自动连接宿主机Ollama：
```bash
docker run -d \
  -p 3000:8080 \
  -v open-webui:/app/backend/data \
  --name open-webui \
  --restart always \
  --add-host=host.docker.internal:host-gateway \
  registry.cn-hangzhou.aliyuncs.com/open-webui/open-webui:main
```
- 访问地址：`http://localhost:3000`
- 首次打开注册管理员账号，无需额外配置即可读取本地Ollama全部模型

### 方案2：无Docker轻量化部署（磁盘不足首选，占用更小）
```bash
# 安装依赖
pip install open-webui
# 启动WebUI服务
open-webui serve
```
访问：`http://127.0.0.1:8080`

### Open WebUI 模型可视化操作全流程
#### 1. 模型下载（图形化替代ollama pull）
1. 左侧菜单栏 → **Models**
2. 顶部搜索框输入模型名（qwen2.5、llama3.2、deepseek-r1）
3. 选择量化版本（7b-q4_K_M），点击 `Pull` 一键下载，实时显示进度



#### 2. 本地模型管理
- **删除模型**：Models页面对应模型右侧 `Delete`，等价 `ollama rm`
- **查看模型信息**：点击模型卡片，查看参数、量化、模板配置
- **切换对话模型**：聊天页面顶部下拉框一键切换已下载模型

#### 3. 模型参数可视化调节（无需命令行）
聊天界面侧边设置可调整：
- Temperature（创意度）
- Top P、上下文窗口长度
- 系统提示词（自定义AI角色）
- 最大生成长度

#### 4. 高级功能
- RAG知识库：上传PDF/TXT本地文档问答
- 多模态：支持Qwen-VL、LLaVA识图对话
- 多用户账号、对话文件夹分类、对话导出
- 局域网共享：修改启动命令允许外网访问，手机/其他电脑连接

## 五、常见问题与优化
### 1. Docker拉取open-webui镜像失败（你遇到的报错）
报错：`Unable to find image 'ghcr.io/open-webui/open-webui:main' locally`
原因：国内无法直连ghcr.io，使用上文**阿里云镜像docker命令**即可解决。

### 2. 模型下载慢、中断
1. Ollama配置代理加速下载
2. 更换国内同步模型（qwen系列阿里云镜像）
3. 中断后重新执行 `ollama pull` 自动断点续传

### 3. 显存不足、运行卡顿
- 选择更小参数量、更低量化版本（q4_K_M优先）
- WebUI设置减小上下文窗口长度
- 关闭其他占用显卡程序

### 4. WebUI无法连接Ollama
Docker部署必须保留参数 `--add-host=host.docker.internal:host-gateway`；
pip部署默认本地11434端口，无需额外配置。

## 六、新手快速上手流程
1. 安装Ollama，验证 `ollama --version`
2. 下载轻量中文模型：`ollama pull qwen2.5:7b`
3. 测试终端对话：`ollama run qwen2.5:7b`
4. 部署Open WebUI可视化面板
5. 浏览器打开3000端口，图形化管理模型、聊天、配置参数

需要我把这份文档导出为**可复制纯命令速查表**，方便你终端快速查阅吗？